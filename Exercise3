/*	Partner(s) Name & E-mail: None
 *	Lab Section: 023
 *	Assignment: Lab #3  Exercise #3 
 *	Exercise Description: [optional - include for your own benefit]
 *	
 *	I acknowledge all content contained herein, excluding template or example
 *	code, is my own original work..
 */


#include <avr/io.h>

int main(void){
        DDRB = 0xFF; // Configure port B's 8 pins as outputs
        PORTA = 0x00;
        PORTC = 0x00;
        Light = 0x01;
	Key_Inserted = 0x02;
	D_Seatbelt = 0x03;
	P_Seatbelt = 0x04;

        while(1)
{
        if(PORTA0||PORT1||PORT2||PORT3 = 1||2)
        {
        PORTC5 && PORT6 =Light;
        scanf("Low fuel");
        }

        if(PORTA0||PORT1||PORT2||PORT3  = 3||4)
        {
        PORTC5 && PORTC6 && PORTC4 =Light;
        scanf("Low fuel");
        }

        if(PORTA0||PORT1||PORT2||PORT3  = 5||6)
        {
        PORT5 && PORT4 && PORT3 =Light;
        }

        if(PORTA0||PORT1||PORT2||PORT3  = 7||8||9)
        {
        PORT5 && PORT4 && PORT3 && PORT2 =Light;
        }

        if(PORTA0||PORT1||PORT2||PORT3  = 10||11||12)
        {
        PORT5 && PORT4 && PORT3 && PORT2 && PORT1 =Light;
        }

        if(PORTA0||PORT1||PORT2||PORT3  = 13||14||15)
        {
        PORT5 && PORT4 && PORT3 && PORT2 && PORT1 && PORT0=Light;
        }

	if(PORTA4 = Key_Inserted)
	{
	 PORTA4 = Light;
	}
	
	if(PORTA5 = D_Seatbelt)
	{
	PORTA5= Light;
	}

	if(PORTA6 = P_Seatbelt)
	{
	PORTA6 = Light;
	}
	
	if(PORTA7 = Key_Inserted && D_Seatbelt && !P_Seatbelt)
	{
	printf("Fasten Seatbelt");
	}


        return 1;
}




